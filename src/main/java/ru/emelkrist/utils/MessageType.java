package ru.emelkrist.utils;

public enum MessageType {
    WHOISIN, COMMUNICATION, LOGOUT

    // WHOISIN to receive the list of the users connected
    // MESSAGE an ordinary text message
    // LOGOUT to disconnect from the Server
}
